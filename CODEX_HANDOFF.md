# 《今天吃什么》软萌盲盒抽奖网页

## Codex 深入开发交接文档

- 项目阶段：可运行单文件原型 → 视觉与工程深入开发
- 当前主文件：`index.html`
- 参考界面：`references/ui-reference.png`
- 初始四状态概念图：`references/original-storyboard.png`
- 目标设备：手机浏览器，优先 iPhone Safari，同时兼容 Android Chrome
- 目标画幅：竖版 9:16，也就是宽高比约为 `9 / 16`，不要改成横版 16:9
- 主要语言：中文

---

## 1. 项目定位

这是一个帮助用户决定“今天吃什么”的轻量手机网页。用户先选择或编辑候选美食，候选项会变成纸条飞入软萌抽奖盒；用户点击抽奖后，盒子摇晃并随机抽出一张纸条，最后展示今天要吃的食物。

产品核心不是复杂功能，而是：

1. 用非常直观的四步流程解决“吃什么”的纠结。
2. 通过软萌盲盒、纸条动画、摇晃反馈和结果揭晓营造仪式感。
3. 页面打开即可使用，不依赖登录、后端或安装。
4. 视觉必须有较高完成度，像一款完整的小型手机应用，而不是普通表单网页。

---

## 2. 已确认的核心需求

### 2.1 完整流程

必须保留以下四个状态，不能删减成一次点击直接出结果：

#### 状态 1：初始选择

- 页面标题为“今天吃什么呢？”
- 显示本次参与抽奖的美食选项。
- 用户可以点按某项，将其加入或移出本次抽奖。
- 用户可以进入编辑面板，新增或删除自定义选项。
- 至少选择两项后才能开始。
- 页面下半部分显示软萌抽奖盒和“开始抽奖”按钮。

#### 状态 2：纸条飞入盒中

- 用户选中的美食变成一张张小纸条。
- 每张纸条带有食物名称和抽象简笔画图标。
- 纸条从不同位置依次飞向抽奖盒入口。
- 盒口发光，强化“装入盲盒”的反馈。
- 动画结束后自动进入抽奖状态。

#### 状态 3：抽奖中

- 页面明确显示“摇一摇中”或类似状态文案。
- 用户点击抽奖按钮后，盒子左右晃动并略微旋转。
- 同时出现手绘感运动线、轻量音效和支持设备上的震动反馈。
- 动画过程中按钮需要防止重复点击。
- 随机结果应从当前已选择的候选项中产生。

#### 状态 4：结果展示

- 抽奖盒打开。
- 抽中的纸条从盒子中弹出。
- 显示食物名称和对应简笔画。
- 显示庆祝标题，例如“恭喜你抽中”。
- 提供“再抽一次”和“重新选择”两个操作。

### 2.2 美食内容

默认候选项目前为：

- 火锅
- 寿司
- 炸鸡
- 拉面
- 披萨
- 汉堡
- 麻辣烫
- 奶茶

编辑要求：

- 支持增加自定义项目。
- 支持删除现有项目。
- 最少保留两项。
- 最多十二项。
- 自定义内容保存到浏览器本地，下次打开仍然保留。
- 美食图片不能使用写实照片，统一使用抽象、可爱、线条简洁的简笔插画。

---

## 3. 视觉方向

### 3.1 总体风格

关键词：

- 超级可爱
- 软萌
- 奶油感
- 粉色系
- 年会盲盒的轻松仪式感
- 手绘插画感
- 完成度高，但不要堆积无意义装饰

参考图的核心视觉特征：

- 奶油白与浅粉色为大面积背景。
- 珊瑚粉、蜜桃粉、奶油黄作为主要强调色。
- 深棕红用于文字和轮廓，避免纯黑。
- 轮廓柔和，圆角大量使用。
- 页面下方有浅粉色棋盘格地面。
- 星星、糖粒、爱心、小圆点等装饰分散在空白区域。
- 抽奖盒是主视觉，使用粉色半透明盒身和奶油色小熊。
- 纸条采用奶油黄便签纸造型。
- 文字是清晰的真实网页文字，而不是烧录在图片里的文字。

### 3.2 字体与文字处理

文字必须通过 HTML 文本和 CSS 构建，禁止把中文标题和按钮文字做进图片中。

原因：

- 保证手机端清晰度。
- 便于后续修改文案。
- 支持无障碍和缩放。
- 避免生成图片中的错字、糊字和字体风格不统一。

建议字体栈：

```css
font-family:
  "PingFang SC",
  "Hiragino Sans GB",
  "Microsoft YaHei",
  system-ui,
  -apple-system,
  sans-serif;
```

文字层级：

- 主标题：粗圆、深棕红、轻微白色阴影，不能使用锐利黑体。
- 步骤胶囊：白字、粉色渐变底、底部轻投影。
- 正文与说明：深棕红或浅棕红。
- 按钮：字重高、字距略宽，优先保证可读性。
- 不要通过过度描边或厚重阴影模拟卡通字，避免廉价感。

### 3.3 UI 组件风格

- 所有主要容器使用大圆角。
- 选择区使用浅白半透明面板与粉色虚线边框。
- 美食纸条采用浅黄渐变、细棕色边框和顶部小图钉。
- 主按钮使用奶油黄渐变、橙黄色边框和底部压层阴影。
- 次按钮使用浅粉渐变。
- 弹层使用底部抽屉，不打开新的浏览器页面。
- 选中状态要清楚但柔和，不能只靠颜色差异。

---

## 4. 素材与布局规划

### 4.1 最底层背景

背景不建议使用一整张截图作为网页背景。当前采用 CSS 构建，后续继续保持可缩放方案：

1. 奶油白至浅粉色的纵向渐变。
2. 非常轻的纸张颗粒点阵。
3. 页面底部浅粉色棋盘格。
4. 局部径向白光，增强奶油感。

这些应作为独立背景层放在内容下方，不能影响点击。

### 4.2 需要作为独立插画或矢量素材的元素

以下元素适合使用 SVG、透明 PNG 或 CSS + SVG 混合实现：

- 抽奖盒主体。
- 小熊脸和结果页小熊角色。
- 食物简笔画图标。
- 奶油黄纸条。
- 星星、爱心、糖粒、闪光等装饰。
- 结果页缎带。

优先级：

1. 优先使用 SVG，保证移动端缩放清晰。
2. 需要明显手绘纹理时，可以使用透明 WebP 或 PNG。
3. 不要把包含文字的整块 UI 做成图片。
4. 不要把整张参考图直接垫在底层模拟界面。

### 4.3 应由代码生成的动态特效

以下效果应使用 CSS、SVG 或 Canvas 动画生成，不要使用固定视频：

- 纸条从不同起点飞入盒口。
- 盒口黄色柔光。
- 抽奖盒左右摇晃和轻微旋转。
- 摇晃时左右运动线。
- 结果纸条向上弹出。
- 结果页放射状光芒。
- 小星星轻微漂浮。
- 按钮按下和弹起反馈。

### 4.4 层级顺序

从底到顶建议为：

1. 页面底色和渐变。
2. 纸张颗粒与棋盘格背景。
3. 装饰星星、糖粒和光斑。
4. 内容面板和标题。
5. 美食纸条与抽奖盒。
6. 动画特效、运动线、发光层。
7. 弹窗、Toast 和交互反馈。

---

## 5. 当前成果

### 5.1 当前文件

`index.html` 是目前的主版本，已经是一个不依赖构建工具的单文件网页。

当前优势：

- 双击文件即可打开。
- 不需要服务器或网络请求。
- CSS、HTML、SVG 图标和 JavaScript 全部内嵌。
- 便于直接发给他人试玩。
- 后续也可迁移到 React、Vue 或 Vite 项目。

### 5.2 已完成的功能

- 竖版手机界面。
- 四步完整抽奖流程。
- 候选食物点按筛选。
- 编辑、美食新增和删除。
- 至少两项、最多十二项的限制。
- 浏览器 `localStorage` 保存候选内容。
- 纸条飞入动画。
- 抽奖盒摇晃动画。
- 随机结果生成。
- 结果纸条弹出。
- 重新抽奖和重新选择。
- Web Audio API 生成轻量交互声音。
- 支持设备上的 `navigator.vibrate` 震动反馈。
- `prefers-reduced-motion` 动画减弱支持。
- 手机安全区域 `env(safe-area-inset-*)` 基础适配。
- 无外部图片依赖的 SVG 食物简笔画。

### 5.3 当前视觉实现

- 奶油粉渐变背景。
- 细颗粒纸张纹理。
- 底部粉色棋盘格。
- 粉色步骤胶囊。
- 奶油黄色美食纸条。
- 粉色小熊抽奖盒。
- 黄色主按钮和粉色次按钮。
- 纸条飞入、盒口发光、摇动线、结果放射光等效果。

### 5.4 当前实现方式

当前代码仍集中在单个 HTML 中：

- HTML：四个 `section.screen` 状态页。
- CSS：完整视觉、响应式和动画。
- JavaScript：数据、状态切换、编辑、随机抽奖、音效和本地保存。
- SVG：美食图标与小熊插画。

当前状态切换由 `showScreen(name)` 控制，页面状态为：

```text
select → fly → draw → result
```

---

## 6. 当前版本仍需深入优化的问题

Codex 不要推倒重写后再慢慢恢复功能。应先运行并理解当前 `index.html`，保留已完成流程，再逐步重构。

### 6.1 视觉精度

- 抽奖盒的轮廓、材质和小熊比例仍可继续向参考图靠近。
- 手绘感还不够统一，目前部分结构偏标准 CSS 几何图形。
- 装饰元素分布需要更有节奏，避免随机散落感。
- 标题字形仍依赖系统字体，需通过字重、字距、轻微变形和布局达到更软萌的效果，但不能牺牲清晰度。
- 结果页小熊和缎带可继续精修。
- 美食图标风格需要统一线宽、颜色数量和构图占比。

### 6.2 页面比例和响应式

需要重点测试：

- iPhone Safari 地址栏展开和收起。
- `100vh` 与 `100dvh` 差异。
- iPhone 刘海与底部 Home Indicator。
- 较矮屏幕，例如 667px 高度。
- Android Chrome 不同长宽比。
- 横屏时不要让核心功能完全不可用，可以提示旋转回竖屏。
- 页面不应出现意外的浏览器横向滚动。
- 所有按钮必须保持足够点击面积。

### 6.3 动画质量

- 纸条飞入轨迹可增加弧线、轻微摆动、不同延迟和前后层级。
- 纸条进入盒口时应有缩小和遮挡关系，而不是直接淡出。
- 摇盒动画可增加蓄力、快速抖动、减速停止三个阶段。
- 结果弹出时可以增加盒盖打开、光效增强、纸条上升和庆祝元素四段节奏。
- 动画不能过慢，完整流程应保持轻快。
- 必须防止快速点击导致状态错乱。

### 6.4 工程结构

单文件适合试玩，但深入开发后建议进行模块化。重构时不能改变已有交互结果。

建议先拆为原生模块，或迁移到 Vite + TypeScript：

```text
src/
├─ main.ts
├─ app/
│  ├─ state.ts
│  ├─ lottery.ts
│  └─ storage.ts
├─ components/
│  ├─ Header.ts
│  ├─ FoodGrid.ts
│  ├─ FoodSlip.ts
│  ├─ LotteryBox.ts
│  ├─ ResultCard.ts
│  └─ EditSheet.ts
├─ animation/
│  ├─ paperFlight.ts
│  ├─ boxShake.ts
│  └─ resultReveal.ts
├─ audio/
│  └─ sound.ts
├─ assets/
│  ├─ icons/
│  └─ decorations/
└─ styles/
   ├─ tokens.css
   ├─ base.css
   ├─ components.css
   └─ animations.css
```

若迁移到框架，优先 Vite + TypeScript。不要引入沉重 UI 组件库，不要破坏当前轻量、可离线运行的特点。

---

## 7. Codex 开发任务优先级

### P0：必须完成

1. 打开并运行当前 `index.html`，确认四步流程全部可用。
2. 建立版本控制，当前版本作为基线提交，不覆盖。
3. 优化手机 Safari 下的尺寸、安全区域和点击问题。
4. 重构状态管理，避免连续点击导致重复动画和错误切屏。
5. 精修抽奖盒、小熊、纸条、结果页和美食图标的视觉一致性。
6. 保持所有中文为真实 HTML 文字。
7. 确保候选项编辑和本地保存不丢失。
8. 增加基本错误处理和空状态提示。
9. 完成真机或设备模拟器测试。
10. 保持可直接发布为静态网页。

### P1：重要优化

1. 将动画拆成清晰的时间线和状态机。
2. 提升纸条飞入盒口的空间关系和遮挡效果。
3. 统一设计 Token，包括颜色、圆角、边框、阴影、字重和间距。
4. 增加声音开关，默认保持轻量，不要自动播放长音乐。
5. 增加“恢复默认美食”入口。
6. 优化键盘弹起时编辑抽屉的布局。
7. 增强无障碍：按钮标签、焦点态、色彩对比、减少动画模式。
8. 增加基础单元测试和交互测试。

### P2：后续可选

以下不是第一轮必须项，完成 P0、P1 后再评估：

- PWA 离线安装。
- 分享抽奖结果图片。
- 最近抽奖记录。
- 多套主题皮肤。
- 用户自定义抽奖盒名称。
- 更多简笔画图标自动匹配。

不要因为开发 P2 而延后 P0 的视觉和手机端稳定性。

---

## 8. 明确禁止事项

- 不要把整张参考图当网页背景。
- 不要把标题、按钮和结果文字做进图片里。
- 不要改成写实美食照片。
- 不要改成复杂游戏、轮盘、卡牌或回合制玩法。
- 不要删除纸条飞入和摇盒过程，直接跳到结果。
- 不要把页面改成横版。
- 不要为了重构而删除当前已能运行的功能。
- 不要引入登录、后端和数据库作为第一阶段依赖。
- 不要使用过多外部库造成页面加载变慢。
- 不要堆积大量没有交互意义的装饰细节。
- 不要使用纯黑色粗描边或高饱和霓虹配色。

---

## 9. 数据与随机逻辑

基础数据结构建议保持简单：

```ts
type FoodOption = {
  id: string;
  name: string;
  icon: string;
  selected: boolean;
};
```

抽奖条件：

- 候选项少于两项时不允许抽奖。
- 从 `selected === true` 的项目中等概率抽取一项。
- 抽奖动画开始时锁定本轮候选数组，避免用户在动画中修改数据。
- 动画完成前不允许重复触发。
- “再抽一次”保留当前候选项。
- “重新选择”返回选择页面，不清空已保存内容。

当前不需要复杂权重算法。若未来加入权重，必须在 UI 中明确展示，不能暗中调整概率。

---

## 10. 验收标准

### 10.1 功能验收

- 可以选择和取消美食。
- 选择少于两项时按钮不可用或给出明确提示。
- 可以新增、删除并保存自定义美食。
- 刷新页面后自定义内容仍存在。
- 点击开始后纸条依次飞入盒子。
- 进入抽奖页后可触发摇晃。
- 每轮只产生一个合法结果。
- “再抽一次”可继续抽取。
- “重新选择”可返回初始状态。
- 快速连续点击不会出现重叠界面或多个结果。

### 10.2 视觉验收

- 手机首屏无明显裁切和横向溢出。
- 主标题清晰，不糊、不变形、不出现错字。
- 抽奖盒是画面视觉中心。
- 食物图标统一为非写实简笔画。
- 色彩与参考图保持奶油粉、珊瑚粉、奶油黄、棕红轮廓体系。
- 组件边框、圆角和阴影风格统一。
- 空白区域有呼吸感，不堆满装饰。
- 结果揭晓有明显层级和庆祝感。

### 10.3 设备验收

至少测试：

- iPhone Safari 常见尺寸。
- Android Chrome 常见尺寸。
- 桌面 Chrome 的手机模拟模式。
- 较矮屏幕和较长屏幕。
- 动画减弱系统设置。
- 静音或不支持震动的设备。

---

## 11. Codex 每轮工作方式

每一轮修改都按以下顺序进行：

1. 阅读本交接文档和当前代码。
2. 先描述准备修改的范围，不要立即大面积重写。
3. 建立一个独立 Git 分支或提交点。
4. 只完成一个清晰目标。
5. 运行检查并打开页面测试。
6. 输出修改文件清单。
7. 输出本轮解决的问题。
8. 输出仍存在的问题。
9. 保留可回退版本。

每轮交付信息建议包含：

```text
本轮目标：
修改文件：
完成内容：
测试结果：
视觉变化：
已知问题：
下一步建议：
```

---

## 12. 建议的第一轮 Codex 指令

可以直接把下面这段作为第一次任务发给 Codex：

```text
请先阅读 CODEX_HANDOFF.md，并运行当前 index.html。

本轮不要推倒重写，也不要迁移框架。先把当前版本作为 Git 基线保存，然后完成一次“现状审计”：

1. 检查 select → fly → draw → result 四个状态的功能是否完整。
2. 检查 iPhone Safari 和 Android Chrome 的高度、安全区域、按钮点击和横向溢出问题。
3. 检查快速连续点击是否会导致动画重复、按钮失效或页面状态错乱。
4. 对照 references/ui-reference.png，列出视觉差距，按背景、字体、抽奖盒、纸条、装饰、动画、结果页分类。
5. 给出下一轮的 P0 修改计划和预计涉及文件。

本轮只允许修复会导致页面无法使用的明显 bug，其余先分析，不要大面积改代码。完成后输出审计报告、测试结果和下一轮任务计划。
```

---

## 13. 最终目标

最终应得到一个：

- 手机上打开即用。
- 四步抽奖流程完整。
- 有纸条飞入、摇盒和结果弹出的仪式感。
- 可编辑并保存美食候选项。
- 视觉接近参考图的奶油粉软萌插画风。
- 中文清晰、交互稳定、动画流畅。
- 能作为静态网页直接部署。
- 代码结构清晰，方便后续继续增加主题和功能。

的完整小型网页应用。
